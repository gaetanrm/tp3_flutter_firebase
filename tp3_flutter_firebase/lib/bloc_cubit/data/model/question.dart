class Question {
  String urlImage;
  String questionText;
  bool isCorrect;
  String answerText;

  Question(
      {required this.urlImage,
        required this.questionText,
        required this.isCorrect,
        required this.answerText});
}